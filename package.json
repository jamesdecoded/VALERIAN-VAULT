{
  "name": "soko-storefront",
  "version": "1.0.0",
  "private": true,
  "description": "SOKO storefront - separated frontend and backend",
  "scripts": {
    "start": "cd backend && npm start",
    "check": "cd backend && npm run check && cd ../frontend && npm run check"
  },
  "engines": {
    "node": ">=18"
  }
}
